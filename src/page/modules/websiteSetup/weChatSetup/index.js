import React from 'react'
import {Form,Input, Button, Tabs} from 'antd'
import './index.less'

class webSetup extends React.Component{
    render() {
        return(
            <div className="tabs-box">333</div>
        )
    }
}

webSetup = Form.create()(webSetup);
export default webSetup;